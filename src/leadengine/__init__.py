"""Lead Discovery & Enrichment Engine (strict MVP).

A minimal, deterministic, plugin-based pipeline that discovers public business leads,
enriches them, extracts public contacts, fingerprints technology, scores them, and
exports CSV/JSON. Public data only.
"""

__version__ = "0.1.0"
