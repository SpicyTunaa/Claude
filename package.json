{
  "name": "lumipic-clone",
  "version": "0.1.0",
  "private": true,
  "description": "A LumiPic-style Telegram Mini App for AI face swap, built with Next.js and Express.",
  "workspaces": [
    "apps/web",
    "apps/server"
  ],
  "scripts": {
    "dev": "npm-run-all --parallel dev:web dev:server",
    "dev:web": "npm run dev --workspace apps/web",
    "dev:server": "npm run dev --workspace apps/server",
    "build": "npm run build --workspace apps/server && npm run build --workspace apps/web",
    "lint": "npm run lint --workspace apps/web"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  },
  "engines": {
    "node": ">=18.18.0"
  }
}
